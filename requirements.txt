# Flask and deps
Flask==0.11
blinker==1.4        # for signals with flask-dance below
Flask-Dance==0.8.3  # For OAuth2

# Data connections - We use the latest code for gludb and currently grab deps
# for both mongo and postgres
git+https://github.com/memphis-iis/GLUDB
pymongo==3.2.2
psycopg2==2.6.1

# For remote API access
requests==2.10.0

# For wsgi server (instead of default flask server)
waitress==0.9.0

# For testing
nose==1.3.7
